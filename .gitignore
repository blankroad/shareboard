# Rust
/target
**/target
Cargo.lock.orig

# Node / frontend
node_modules
dist
.svelte-kit
/src-tauri/target

# 배포물 (scripts/build-windows.ps1 산출물)
/release

# Docker Linux 검증 산출물 (scripts/docker/run.sh)
/verify-out

# OS / editor
.DS_Store
*.swp
.idea
.vscode/*
!.vscode/extensions.json

# shareboard runtime data / secrets — 절대 커밋 금지
*.key
*.pem
identity.json
peers.json
workspace.json
history.db*
server.toml.local
/data
/logs
